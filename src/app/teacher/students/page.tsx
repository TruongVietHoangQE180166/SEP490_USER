import { TeacherStudentModule } from '@/modules/teacher-student';

export const metadata = {
  title: 'Quản lý học viên - VIC',
};

export default function TeacherStudentsPage() {
  return <TeacherStudentModule />;
}
